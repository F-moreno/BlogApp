{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<h3>Editar Usuário</h3>
<div class="card">
    <div class="card-body">
        <form action="/admin/usuarios/edit/" method="post">
            <input type="hidden" id="id" name="id" value="{{usuario._id}}">
            <label for="nome">Nome:</label>
            <input type="text" id="nome" name="nome" placeholder="Nome da Categoria" class="form-control" value="{{usuario.nome}}">
            <label for="email">Email:</label>
            <input type="text" id="email" name="email" placeholder="email da Categoria" class="form-control" value="{{usuario.email}}">
            <div class="mt-4">
                {{#if usuario.eAdmin}}
                    <input class="form-check-input" type="checkbox" id="eAdmin" name="eAdmin" checked/>
                {{else}}
                    <input class="form-check-input" type="checkbox" id="eAdmin" name="eAdmin"/> 
                {{/if}}
                <label for="form-check-label" class="form-check-label">Admin</label>
            </div>
            <button type="submit" class="btn btn-success mt-4">Editar Usuario</buton>
        </form>
    </div>
</div>