<h2>Lista de Categorias:</h2>
<hr>
<a href="/usuarios/registro"><button class="btn btn-success">Novo Usuário</button></a>
<div class="container mx-auto mt-4">
    <div class="row">
        {{#each usuarios}}
            <div class="col-md-3 mt-3">
                <div class="card " style="width: 18rem; height: 25rem;">
                    <img src="https://i.imgur.com/ZTkt4I5.jpg" class="card-img-top" alt="...">
                    <div class="card-body">
                        <h5 class="card-title">{{nome}}</h5>
                        <h6 class="card-subtitle mb-2 text-muted">{{email}}</h6>
                        {{#if eAdmin}}
                            <p class="card-text">Administrador do sistema</p>
                        {{/if}}
                        <div class="row">
                                <a href="usuarios/edit/{{_id}}" class="btn btn-success col"> Editar</a>
                                <a href="usuarios/deletar/{{_id}}" class="btn btn-danger col"> Deletar</a>
                        </div>
                    </div>
                </div>
            </div> 

<!--
    <div class="card mt-4">
        <div class="card-body">
            <h4>{{nome}}</h4>
            <small>Email: {{email}}</small><br>
            <small>eAdmin: {{eAdmin}}</small><br>
            <a href="/admin/usuarios/edit/{{_id}}"><button class="btn btn-success mt-4">Editar</button></a>
            
            <form action="/admin/usuarios/deletar/{{_id}}" method="post">
                <button type="submit" class="btn btn-danger mt-2">Deletar</button>
            </form>
        </div>
    </div>    
-->
        {{else}}
            <h4 class="mt-3">Nenhum usuario registrado.</h4>
        {{/each}}
    </div>
</div>