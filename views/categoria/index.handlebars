<h2>Categorias:</h2>
{{#each categorias}}
    <div class="card mt-4">
        <div class="card-body">
            <a href="/categorias/{{slug}}"><h3>{{nome}}</h3></a>
        </div>
    </div>
{{else}}
    <h5>Nenhuma categoria</h5>
{{/each}}